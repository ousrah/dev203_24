1 - Créez les models User, Role,  Post, Article, Comment, Phone et définie les relations selon le modèle relationnel suivant : 

l'utilisateur ne peut avoir q'un seul téléphone et le téléphone ne peut être affecté qu'a un seul utilisateur
chaque utilisateur peut avoir plusieurs roles et le role peut être affecte a plusieurs utilisateurs
chaque post peut avoir plusieurs commentaires et un commentaire peut concerner zero ou un post
chaque article peut avoir plusieurs commentaires et un commentaire peut concerner zero ou un article
chaque utilisateur peut ecrire plusieurs commentaires et un commentaire ne peut être ecris que par un et un seul utilisateur



Users (id, name, email, password)
Roles (id, name, description)
UserRoles(user_id, role_id)
Phones(id,number, user_id)
Posts(id, title, description, image)
Articles(id, title, description, image)
Comments(id, comment, post_id, article_id, user_id)



2 - genere la base de données en utilisant 
	php artisan migrate
	php artisan migrate:fresh

3 - C'est quoi la différence entre les deux méthodes ?

4 - découvre dans la documentation officielle les autres possibilité de migrate

5 - créer les factories de chaque modele pour générer les enregistrements 

6 - configure le seeder pour : 
	- générer les roles suivants :  ('admin', 'publisher', 'author', 'guest')
	- créer 10 utilisateurs avec chaqun deux roles aleatoires
	- créer 10 posts et 10 articles avec des images -fak()->urlImage() et pour chaque post/article générer 10 commentaires.

7 - modifie la page d'accueil pour qu'elle affiche les articles dans une colonne et les posts dans une autre colonne responsives

8 - Ajoute dans le header de ton dashborad une zone de texte de recherche avec la possiblité de faire entrer pour lancer la recherche javascript avec axios ou ajax, le resultat de la recherche doit être affiché sans rafraichir la page en entier

9 - le clique sur le titre doit afficher la page de l'article/post selectionné avec tous ces commentaire

10 - le clique sur le nom de l'auteur affiche tous les artiesl/posts rédigés par lui
